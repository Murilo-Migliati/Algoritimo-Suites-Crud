@model IEnumerable<Algoritimo_Suites.Models.Cliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Clientes</h1>

<p>
    <a asp-action="NovoCliente"class="btn btn-success">Novo Cliente</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                    <!-- Contêiner para alinhar os botões -->
                    <div class="button-group">
                        <!-- Link para Editar -->
                        <a asp-action="EditarCliente" asp-route-id="@item.Id" class="btn btn-outline-primary">Editar</a>

                        <!-- Link para Detalhes -->
                        <a asp-action="DetalhesCliente" asp-route-id="@item.Id" class="btn btn-outline-primary">Detalhes</a>

                        <!-- Formulário para Deletar -->
                        <form asp-action="DeletarCliente" asp-route-id="@item.Id" method="post" style="display:inline;">
                            <button type="submit" class="btn btn-danger">Deletar</button>
                        </form>
                    </div>
            </td>
        </tr>
}
    </tbody>
</table>
